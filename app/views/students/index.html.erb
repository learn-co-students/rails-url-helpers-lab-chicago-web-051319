<h1> Hello World </h1>

<div>
  <% @students.each do |student| %>
   <!-- ORIGINAL  <div> <%= student.first_name + " " + student.last_name %> </div> -->
   <div> 
    <%= link_to student.first_name + " " + student.last_name , student_path(student) %>

  </div>

  <% end %>
</div>